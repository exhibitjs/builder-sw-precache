{
  "name": "exhibit-builder-sw-precache",
  "description": "Generates a service worker using the Chrome team's sw-precache template",
  "version": "0.1.0",
  "author": "Callum Locke",
  "dependencies": {
    "sw-precache": "^2.2.0"
  },
  "devDependencies": {
    "babel": "^5.8.23",
    "babel-eslint": "^4.1.3",
    "eslint": "^1.6.0",
    "eslint-config-exhibit": "^1.0.0",
    "eslint-plugin-babel": "^2.1.1",
    "rimraf": "^2.4.3"
  },
  "files": [
    "dist",
    "src"
  ],
  "license": "MIT",
  "main": "dist/index.js",
  "repository": "exhibitjs/builder-sw-precache",
  "scripts": {
    "build": "rimraf dist && babel src --out-dir dist",
    "buildwatch": "rimraf dist && babel src --out-dir dist --watch",
    "prepublish": "npm run build",
    "test": "eslint ."
  }
}
